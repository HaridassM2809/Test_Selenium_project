package javaBasic;

public interface TestInterface {

//	private interface Test2{
		
//	} we can't create another interface inside one interface, only we can create default interface
	interface Test2{
		
	}
}
//public interface Test3{
	
//} we can't create other than default interface outside of interface and inside of one interface.
interface Test4{
	
}// we can create a default interface outside the interface also.

